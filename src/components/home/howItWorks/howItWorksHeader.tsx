'use client';

import SectionHeader from '../../ui/sectionHeader';

export default function HowItWorksHeader() {
  return (
    <SectionHeader
    alignment="left"
      title="How It Works"
      description="Simple steps to get your IT solutions up and running."
    />
  );
}
